<view class="padding-lr-sm padding-top-sm">
  <view class="padding  bg-blue radius-sm">
    <free-image src="/static/image/my/logoBig.png" width="200rpx" height="80rpx" ></free-image>
    <view class="">一款轻量、开箱即用的原生微信小程序UI组件库</view>
  </view>
</view>
<view class="">
  <view class="padding-lr-sm padding-top-sm flex  align-center">
    <view class="text-bold text-gray padding-left-xs">基础样式</view>
  </view>
  <navigator wx:for="{{basicStyle}}" wx:key="index" class="" url="/pages/basic/{{item.title}}/{{item.title}}" hover-class="none">
    <free-list arrow="true" bind:click="detail" >
      <view class="free-list-name text-bold  padding-right-xs">{{item.name}}</view>
      <view class="text-Abc  text-bold">{{item.title}}</view>
    </free-list>
  </navigator>
  <view class="padding-lr-sm padding-top-sm flex  align-center">
    <view class="text-bold text-gray padding-left-xs">基础组件</view>
  </view>
  <navigator wx:for="{{basicCompent}}" wx:key="index" class="" url="/pages/basic/{{item.title}}/{{item.title}}" hover-class="none">
    <free-list arrow="true" bind:click="detail" >
      <view class="free-list-name text-bold padding-right-xs">{{item.name}}</view>
      <view class="text-Abc  text-bold">{{item.title}}</view>
    </free-list>
  </navigator>
</view> 